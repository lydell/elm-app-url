{
  "private": true,
  "type": "module",
  "scripts": {
    "start": "run-pty % elm-test --watch % elm-review --fix --watch % elm-doc-preview --no-browser % elm-watch hot % node dev-server.js",
    "test": "run-pty --auto-exit % prettier --check . % elm-format --validate src tests example % elm-review % elm-test % elm-watch make --optimize",
    "benchmarks": "cd benchmarks && elm make src/Benchmarks.elm"
  },
  "devDependencies": {
    "elm-doc-preview": "5.0.5",
    "elm-review": "2.9.1",
    "elm-test": "0.19.1-revision11",
    "elm-tooling": "1.12.0",
    "elm-watch": "1.1.2",
    "prettier": "2.8.3",
    "run-pty": "4.0.3"
  }
}
